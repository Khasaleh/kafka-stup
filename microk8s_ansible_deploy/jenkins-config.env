# Jenkins Configuration
# Update these values with your actual Jenkins configuration

JENKINS_URL=http://192.168.1.224:8080
JENKINS_USER=khaled
JENKINS_PASSWORD=Welcome123
JENKINS_TOKEN=your_jenkins_token

# Jenkins Job Names (update these to match your actual job names)
INFRASTRUCTURE_JOB=infrastructure-build
APPLICATION_JOB=application-build
FRONTEND_JOB=frontend-build
DEPLOYMENT_JOB=deploy-to-kubernetes
FULL_PIPELINE_JOB=full-pipeline

# Build Parameters
DEFAULT_ENVIRONMENT=dev
DEFAULT_IMAGE_TAG=latest

# Timeout Settings (in seconds)
INFRASTRUCTURE_TIMEOUT=1800
APPLICATION_TIMEOUT=1800
FRONTEND_TIMEOUT=1200
DEPLOYMENT_TIMEOUT=2400
FULL_PIPELINE_TIMEOUT=3600 