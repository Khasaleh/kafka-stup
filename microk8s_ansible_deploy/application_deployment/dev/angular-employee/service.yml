apiVersion: v1
kind: Service
metadata:
  name: angular-employee
  namespace: default
  labels:
    app: angular-employee
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: angular-employee
