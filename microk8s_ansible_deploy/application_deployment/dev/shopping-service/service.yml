apiVersion: v1
kind: Service
metadata:
  name: shopping-service
  namespace: default
  labels:
    app: shopping-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: shopping-service
