apiVersion: v1
kind: Service
metadata:
  name: employees-service
  namespace: default
  labels:
    app: employees-service
spec:
  type: ClusterIP
  ports:
    - port: 8088
      targetPort: 8088
      protocol: TCP
      name: http
  selector:
    app: employees-service
