apiVersion: v1
kind: Service
metadata:
  name: promotion-service
  namespace: default
  labels:
    app: promotion-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: promotion-service
