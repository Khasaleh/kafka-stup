apiVersion: v1
kind: Service
metadata:
  name: fazeal-business-management
  namespace: default
  labels:
    app: fazeal-business-management
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: fazeal-business-management
