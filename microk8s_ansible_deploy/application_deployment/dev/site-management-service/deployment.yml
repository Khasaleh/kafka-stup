apiVersion: apps/v1
kind: Deployment
metadata:
  name: site-management-service
  namespace: default
  labels:
    app: site-management-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: site-management-service
  template:
    metadata:
      labels:
        app: site-management-service
    spec:
      containers:
        - name: site-management-service
          image: khsaleh889/familymicroservices:$IMAGE
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8103
              name: http
          envFrom:
            - configMapRef:
                name: site-management-service-config
            - secretRef:
                name: site-management-service-secret
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8103
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8103 
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
      imagePullSecrets:
        - name: dockerhub-secret
