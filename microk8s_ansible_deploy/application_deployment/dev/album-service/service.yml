apiVersion: v1
kind: Service
metadata:
  name: album-service
  namespace: default
  labels:
    app: album-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: album-service
