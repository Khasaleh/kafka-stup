apiVersion: v1
kind: Service
metadata:
  name: chat-app
  namespace: default
  labels:
    app: chat-app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: chat-app