apiVersion: v1
kind: Service
metadata:
  name: ads-service
  namespace: default
  labels:
    app: ads-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: ads-service